{
  "name": "@nextbot/testing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Test factories, fixture tenants, MCP mock server, ADK stubs shared across the workspace's test suites.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --dir ."
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
