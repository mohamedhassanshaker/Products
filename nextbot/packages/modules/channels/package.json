{
  "name": "@nextbot/channels",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BL-04: channel CRUD (WebWidget this phase), channel_capability reference data (LLD §3.4).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --dir .",
    "test:integration": "vitest run --project integration --dir ."
  },
  "dependencies": {
    "@nextbot/channel-adapters": "workspace:*",
    "@nextbot/contracts": "workspace:*",
    "@nextbot/db": "workspace:*",
    "@nextbot/secrets": "workspace:*",
    "@nextbot/tenancy": "workspace:*",
    "drizzle-orm": "^0.36.1"
  },
  "devDependencies": {
    "@nextbot/testing": "workspace:*",
    "@types/pg": "^8.11.10",
    "pg": "^8.13.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
