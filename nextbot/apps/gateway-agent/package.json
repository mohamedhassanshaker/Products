{
  "name": "nextbot-gateway-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "On-prem stdio<->HTTPS tunnel binary, ships to tenants (BL-22).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --dir ."
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
