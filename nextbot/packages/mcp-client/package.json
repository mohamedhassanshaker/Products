{
  "name": "@nextbot/mcp-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP transport, list_tools, call_tool, Ajv schema validation (LLD §5.4, §1.1). Ajv is scoped exclusively to this package.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --dir .",
    "test:integration": "vitest run --project integration --dir ."
  },
  "dependencies": {
    "@nextbot/contracts": "workspace:*",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@nextbot/testing": "workspace:*",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
