{
  "name": "nextbot-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DATA PLANE — ADK executor + turn/resume BullMQ consumers. No outbound egress (ADR-0004).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --dir ."
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
