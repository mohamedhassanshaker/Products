{
  "name": "@nextbot/secrets",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SecretsProvider port + Vault/KMS/env implementations (ADR-0007 envelope encryption).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --dir ."
  },
  "dependencies": {
    "@nextbot/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
