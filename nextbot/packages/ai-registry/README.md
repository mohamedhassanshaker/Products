# @nextbot/ai-registry

Provider-agnostic model registry (LLD §7.1). ONLY package that may import @google/adk or a provider SDK — enforced by dependency-cruiser.

Reserved scaffold (Phase 0) — populated in the dev phase that first needs it.
