{
  "name": "@nextbot/telemetry-export",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Target Architecture Blueprint Phase 18 (BL-49, FR-ADM-10) — tenant-scoped, opt-in OpenTelemetry trace/metric export and audit-log SIEM streaming, additive to the existing in-console Runtime Traces/Audit Log experiences.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --dir .",
    "test:integration": "vitest run --project integration --dir ."
  },
  "dependencies": {
    "@nextbot/contracts": "workspace:*",
    "@nextbot/db": "workspace:*",
    "@nextbot/observability": "workspace:*",
    "@nextbot/tenancy": "workspace:*",
    "drizzle-orm": "^0.36.1"
  },
  "devDependencies": {
    "@nextbot/agent-platform": "workspace:*",
    "@nextbot/audit": "workspace:*",
    "@nextbot/testing": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
