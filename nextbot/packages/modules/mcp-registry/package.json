{
  "name": "@nextbot/mcp-registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 6 (BL-29, ADR-0014): MCP manifest pinning + drift-as-new-item quarantine. LLD §14.3 (Module A), scoped per this module's own doc comment.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --dir .",
    "test:integration": "vitest run --project integration --dir ."
  },
  "dependencies": {
    "@nextbot/connectors": "workspace:*",
    "@nextbot/contracts": "workspace:*",
    "@nextbot/db": "workspace:*",
    "@nextbot/mcp-client": "workspace:*",
    "@nextbot/secrets": "workspace:*",
    "@nextbot/tool-registry": "workspace:*",
    "drizzle-orm": "^0.36.1"
  },
  "devDependencies": {
    "@nextbot/testing": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
