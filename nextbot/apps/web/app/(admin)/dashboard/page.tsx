/** Reporting rollups land in a later phase (BL-17) — this is the Phase 3 shell
 * placeholder, converted off Chakra in Batch A (Plan Phase 2). */
export default function DashboardPage() {
  return (
    <>
      <h1 className="mb-2 font-heading text-lg font-semibold">Dashboard</h1>
      <p className="text-muted-foreground">
        Reporting rollups land in a later phase (BL-17) — this is the Phase 3 shell placeholder.
      </p>
    </>
  );
}
