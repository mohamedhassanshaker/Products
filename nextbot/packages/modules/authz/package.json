{
  "name": "@nextbot/authz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Target Architecture Blueprint Phase 6 (BL-37, ADR-0012, LLD §14.2) — the centralized permission-intersection evaluator (FR-ORC-02/FR-SEC-08) and the /authz/simulate preview endpoint.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --dir .",
    "test:integration": "vitest run --project integration --dir ."
  },
  "dependencies": {
    "@nextbot/contracts": "workspace:*",
    "@nextbot/db": "workspace:*",
    "@nextbot/skills": "workspace:*",
    "@nextbot/pii": "workspace:*",
    "@sinclair/typebox": "^0.33.15",
    "drizzle-orm": "^0.36.1"
  },
  "devDependencies": {
    "@nextbot/connectors": "workspace:*",
    "@nextbot/testing": "workspace:*",
    "@nextbot/tool-registry": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
